# اجرای محلی پروژه

این نسخه برای اجرای ویندوز به Docker نیاز ندارد. سرویس PostgreSQL و Redis به‌صورت قابل‌حمل در خود پروژه اجرا می‌شوند.

## راه‌اندازی یک‌مرحله‌ای

در پوشه اصلی پروژه روی فایل `start-local.ps1` راست‌کلیک کنید و **Run with PowerShell** را بزنید. اگر اجرای اسکریپت در ویندوز محدود بود، PowerShell را در همین پوشه باز کنید و این دستور را اجرا کنید:

```powershell
powershell -ExecutionPolicy Bypass -File .\start-local.ps1
```

بعد از آماده‌شدن سرویس‌ها، مرورگر روی این نشانی باز می‌شود:

- سایت: http://localhost:3001
- مستندات API: http://localhost:3002/api/docs

## حساب‌های آزمایشی

رمز همه حساب‌ها `demo1234` است. در صفحه ورود دکمه ورود سریع برای همه نقش‌ها وجود دارد.

| نقش | موبایل |
|---|---|
| مدیر ارشد | `09120000000` |
| صاحب باشگاه | `09120000001` |
| ورزشکار | `09120000002` |
| مربی | `09120000003` |
| متخصص تغذیه | `09120000004` |
| پذیرش | `09120000005` |
| بوفه‌دار | `09120000009` |

هر نقش از صفحه `http://localhost:3001/auth/login` درگاه ورود مستقل دارد. داده‌های محلی شامل سه باشگاه، اعضا، عضویت فعال، پرداخت، حضور و غیاب، برنامه تمرینی، رژیم غذایی، سفارش و محصولات بوفه، تبلیغ استانی، دستگاه تردد، اشتراک پلتفرم و مرکز اتصال سامانه‌ها است.
