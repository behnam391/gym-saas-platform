# گُردیار پرو

اپ عملیاتی مدیران و همکاران باشگاه برای نقش‌های مدیر باشگاه، پذیرش، مربی،
مشاور تغذیه و بوفه‌دار.

```powershell
cd mobile
npm run pro
```

آدرس پیش‌فرض API نسخه آنلاین است و برای توسعه محلی می‌توان
`EXPO_PUBLIC_API_BASE_URL` را در فایل `.env` تغییر داد.
